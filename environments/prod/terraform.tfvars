# CI replaces this with the image digest of the commit being deployed. A
# floating tag here would mean a re-apply silently deploys whatever `latest`
# points at now.
container_image = "123456789012.dkr.ecr.eu-west-1.amazonaws.com/app:latest"
